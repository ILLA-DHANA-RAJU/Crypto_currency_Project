services:
  - type: web
    name: crypto-flask
    runtime: python
    plan: free
    region: oregon
    buildCommand: 'pip install -r requirements.txt'
    startCommand: 'gunicorn app:app'
    envVars:
      PYTHON_VERSION: 3.10.11
